# Virtual Helmsman

A modular local voice agent for a ship simulator: listens to spoken English,
replies via speech, and executes ship-simulator actions through remote-LLM tool
calls.

See [`TASK.md`](TASK.md) for the full project brief. This README will be
fleshed out (setup, dependencies, backend switching, latency numbers) as the
implementation lands.
