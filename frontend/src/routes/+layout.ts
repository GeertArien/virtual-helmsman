// SPA mode: no SSR, no prerendering. The dashboard is purely client-side --
// it has nothing to render until it has talked to the live agent.
export const ssr = false;
export const prerender = false;
